# Kurye Özgeçmişi (CV) — Uygulama Planı

> **Ajan işçiler için:** GEREKLİ ALT BECERİ: Bu planı görev görev uygulamak için
> `superpowers:subagent-driven-development` (önerilen) veya
> `superpowers:executing-plans` kullanın. Adımlar takip için `- [ ]` kutucuğu
> kullanır.

**Hedef:** Kurye profillerine platform içi bir özgeçmiş katmanı eklemek; kurye
tek bir sihirbazda CV'sini oluştursun, işveren başvuruyu incelerken
`profil-kurye.html` üzerinde görsün.

**Mimari:** CV verisi `profiles`'a kolon eklenerek değil, ayrı bir
`public.courier_cv` tablosunda tutulur (migration-34'ün `work_experience` için
kurduğu desenin aynısı: sahip RLS'i + `security_invoker = false` görünüm).
Sihirbaz kendi sayfası ve kendi modülüdür (`cv-olustur.html` + `cv.js`);
`profil-duzenle.html`'e sekme olarak eklenmez. İş deneyimi adımı veriyi
kopyalamaz — `supabase.js`'te zaten var olan `add/update/deleteWorkExperience`
ile aynı tabloya yazar.

**Teknoloji:** Framework yok, derleme adımı yok. Düz HTML + CSS + JS.

**Tasarım dokümanı:** `specs/2026-08-19-kurye-cv-design.md`

## Global Kısıtlar

- Kapsam yalnız **`docs/`**. `www/` (Capacitor APK) ve `android/` **hiç
  ellenmez**. APK derleme turu yok.
- **JS dil stili — ölçüldü, uyulacak:** `docs/assets/js/` içinde **0 arrow
  function**, **0 `const`/`let`** var; 309 `await` var. Ev stili: `var` +
  `function` + `async/await`. Yeni kodda `=>`, `const`, `let` **kullanılmaz**.
- **Kaçış zorunlu.** Başka kullanıcıdan gelen hiçbir değer kaçırılmadan DOM'a
  girmez. Metin → `KB.esc()`, attribute → `KB.esc()`, `onclick` içindeki JS
  string → `escJs` kalıbı. `onclick` içinde `esc()` **kullanılmaz**.
- **Uydurma veri yasak.** `SB.isOn()` false ise veya kayıt yoksa boş durum
  gösterilir; sahte sayı/oran üretilmez.
- Yerel sunucu **`npx http-server docs -c-1`**. `npx serve` **YASAK** — query
  string düşürüp `?auth=` parametresini yok ediyor.
- Projede build/lint/typecheck/test **yoktur**. Doğrulama: `node --check`,
  curl probe'ları ve Playwright.
- **Migration'ları ajan çalıştıramaz.** Dosya yazılır, kullanıcıya verilir,
  kullanıcı Supabase SQL Editor'de çalıştırır ve onay verir; ancak ondan sonra
  doğrulama adımı koşulur.
- Her görev sonunda commit atılır. Push yalnız kullanıcı isteyince.
- Sabitler (her curl adımında kullanılacak):

```bash
U="https://fdszypytpodndtlbuzuz.supabase.co/rest/v1"
K="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZkc3p5cHl0cG9kbmR0bGJ1enV6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA0NTUxMjMsImV4cCI6MjA5NjAzMTEyM30.D3Nmk1k2P00_Y8L5flBYR85iuoy7r-3mKeck3QoRVcc"
NONE="00000000-0000-0000-0000-000000000000"
```

---

## Dosya Yapısı

| Dosya | Sorumluluk | Durum |
|---|---|---|
| `supabase/migration-35-profil-eksik-kolonlar.sql` | Yedi eksik kolon (önkoşul) | Yeni |
| `supabase/migration-36-kurye-cv.sql` | `courier_cv` tablo + RLS + görünüm | Yeni |
| `docs/assets/js/cv.js` | `window.KBCV` — sihirbaz motoru, adım render'ları, doğrulama | Yeni |
| `docs/cv-olustur.html` | Sihirbaz sayfası kabuğu | Yeni |
| `docs/assets/js/supabase.js` | `myCv` / `cvFor` / `saveCv` veri katmanı | Değişecek |
| `docs/assets/js/app.js` | Kurye profiline "Özgeçmiş" bölümü | Değişecek |
| `docs/panel-kurye.html` | Sihirbaza giriş kartı | Değişecek |
| `docs/basvurular.html` | Aday kartına SRC / ehliyet çipleri | Değişecek |
| `docs/assets/css/main.css` | `cv-*` sınıfları | Değişecek |
| `docs/assets/js/i18n.js` | Yeni anahtarlar | Değişecek |
| `docs/sitemap.xml` | Yalnız açıklama satırı — sayfa giriş istediği için indekslenmez | Değişecek |
| `CLAUDE.md` | Yeni yüzeyin ve `window.KBCV`'nin kaydı | Değişecek |

`cv.js` neden ayrı: `profil-duzenle.html` 2790 satır ve dört ayrı `<script>`
bloğu taşıyor; blokların ayrı kapsam olması 2026-08-19'da gerçek bir hataya yol
açtı (`esc()` başka bloktaydı). Sihirbaz kendi sınırı olan bir birim olur.

---

### Görev 1: Önkoşul — `profiles`'taki yedi eksik kolon

Bu görev CV'den bağımsız olarak **canlı bir hatayı** düzeltir: Kişisel Bilgiler
ve Araç Bilgileri formları şu an hiç kaydetmiyor.

**Dosyalar:**
- Oluştur: `supabase/migration-35-profil-eksik-kolonlar.sql`

**Arayüzler:**
- Üretir: `profiles.dogum_tarihi`, `.ilce`, `.gunluk_saat`, `.arac_marka`,
  `.arac_model`, `.plaka`, `.plaka_gizli` — Görev 4 ve 7 bunlara dayanır.

- [ ] **Adım 1: Hatanın bugün var olduğunu ölç (temel çizgi)**

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X PATCH "$U/profiles?id=eq.$NONE" \
  -H "apikey: $K" -H "Authorization: Bearer $K" \
  -H "Content-Type: application/json" -d '{"dogum_tarihi":null}'
```

Beklenen: **400** (PGRST204 — kolon yok). Bu, düzeltmenin gerekli olduğunun
kanıtıdır; migration'dan sonra aynı komut 204 dönecek.

- [ ] **Adım 2: Migration dosyasını yaz**

```sql
-- ============================================================
-- Kuryemi Bul — Migration 35: profil formlarının yazdığı eksik kolonlar
--
-- SORUN: docs/profil-duzenle.html üç formda şu alanları public.profiles'a
-- yazıyor ama kolonların hiçbiri yok:
--   dogum_tarihi  ilce  gunluk_saat  arac_marka  arac_model  plaka  plaka_gizli
--
-- PostgREST bunu 400 / PGRST204 ile reddediyor. savePersonal() tek bir UPDATE
-- gönderdiği için aynı istekteki ad, sehir ve aciklama da yazılmıyor —
-- yani Kişisel Bilgiler ve Araç Bilgileri formları hiç kaydetmiyor.
--
-- KULLANIM: Supabase → SQL Editor → Run. Idempotent.
-- ============================================================

alter table public.profiles
  add column if not exists dogum_tarihi date,
  add column if not exists ilce         text    default '',
  add column if not exists gunluk_saat  int,
  add column if not exists arac_marka   text    default '',
  add column if not exists arac_model   text    default '',
  add column if not exists plaka        text    default '',
  -- Plaka kişisel veridir; varsayılan GİZLİ olmalı.
  add column if not exists plaka_gizli  boolean not null default true;

-- Günlük çalışma saati makul aralıkta kalsın (0 = belirtilmemiş).
do $$
begin
  if not exists (
    select 1 from pg_constraint
     where conname = 'profiles_gunluk_saat_araligi'
       and conrelid = 'public.profiles'::regclass
  ) then
    alter table public.profiles
      add constraint profiles_gunluk_saat_araligi
      check (gunluk_saat is null or (gunluk_saat >= 0 and gunluk_saat <= 24));
  end if;
end $$;

-- NOT: public.profiles_public görünümü AÇIK KOLON LİSTESİ kullanıyor
-- (migration-20:97). Bu yedi kolon oraya eklenmediği için misafire
-- sızmaz — görünüme dokunmaya gerek YOK. Bu bilinçli bir tercihtir:
-- dogum_tarihi ve plaka dışarı açılmamalıdır.
```

- [ ] **Adım 3: Commit**

```bash
git add supabase/migration-35-profil-eksik-kolonlar.sql
git commit -m "fix(db): profil formlarinin yazdigi yedi kolon eksikti"
```

- [ ] **Adım 4: Kullanıcıdan çalıştırmasını iste — DUR**

Kullanıcıya dosyanın yolunu ver, Supabase SQL Editor'de çalıştırmasını ve
sonucu bildirmesini iste. Onay gelmeden Adım 5'e geçme.

- [ ] **Adım 5: Yedi kolonu tek tek doğrula**

```bash
for c in dogum_tarihi ilce gunluk_saat arac_marka arac_model plaka plaka_gizli; do
  printf '%-14s ' "$c"
  curl -s -o /dev/null -w '%{http_code}\n' -X PATCH "$U/profiles?id=eq.$NONE" \
    -H "apikey: $K" -H "Authorization: Bearer $K" \
    -H "Content-Type: application/json" -d "{\"$c\":null}"
done
```

Beklenen: yedi satırın **hepsi 204**. Herhangi biri 400 dönerse migration
eksik çalışmıştır — devam etme.

- [ ] **Adım 6: Misafire sızmadığını doğrula**

```bash
curl -s "$U/profiles_public?select=plaka&limit=1" -H "apikey: $K" -H "Authorization: Bearer $K"
```

Beklenen: `42703` hatası (kolon görünümde yok). **200 + veri dönerse** görünüm
sızdırıyor demektir; migration'a görünüm düzeltmesi eklenmeli.

- [ ] **Adım 7: Formun gerçekten kaydettiğini tarayıcıda doğrula**

`npx http-server docs -c-1` ile sunucuyu başlat, kurye hesabıyla giriş yap,
`profil-duzenle.html` → Kişisel Bilgiler → doğum tarihi ve ilçe gir → Kaydet.
Beklenen: hata toast'ı **yok**, sayfa yenilenince değerler duruyor.

- [ ] **Adım 8: Doğrulama sonucunu commit'e ekle**

```bash
git commit --allow-empty -m "test(db): migration-35 dogrulandi - yedi kolon 204, gorunum sizdirmiyor"
```

---

### Görev 2: `courier_cv` tablosu, RLS ve görünüm

**Dosyalar:**
- Oluştur: `supabase/migration-36-kurye-cv.sql`

**Arayüzler:**
- Üretir: `public.courier_cv` tablosu ve `public.courier_cv_public` görünümü —
  Görev 3 bunları okur/yazar.

- [ ] **Adım 1: Migration dosyasını yaz**

```sql
-- ============================================================
-- Kuryemi Bul — Migration 36: kurye özgeçmişi (CV)
--
-- Tasarım: specs/2026-08-19-kurye-cv-design.md
--
-- profiles'a kolon EKLENMEZ: o tablo zaten 30+ kolonla üç rolü taşıyor ve
-- bu alanlar yalnız kuryeyi ilgilendiriyor. Desen migration-34'ün
-- work_experience deseninin aynısı.
--
-- Adli sicil / sağlık raporu / psikoteknik BİLEREK YOK — KVKK m.6 özel
-- nitelikli veri ve platformun ilgi alanı dışında.
--
-- KULLANIM: Supabase → SQL Editor → Run. Idempotent.
-- ============================================================

create table if not exists public.courier_cv (
  id              uuid primary key default gen_random_uuid(),
  profile_id      uuid not null unique references public.profiles(id) on delete cascade,
  user_id         uuid not null references auth.users(id) on delete cascade,

  ozet            text    not null default '',
  ehliyet_sinifi  text[]  not null default '{}',   -- A1 / A2 / A / B
  ehliyet_tarihi  date,
  src_belge       boolean not null default false,  -- 15.05.2026'dan beri zorunlu
  src_gecerlilik  date,
  egitim          jsonb   not null default '[]'::jsonb,  -- [{okul,derece,yil}]
  tercih_bolgeler text[]  not null default '{}',
  musaitlik       text    not null default '',

  yayinlandi      boolean not null default false,
  created_at      timestamptz not null default now(),
  updated_at      timestamptz not null default now()
);

create index if not exists courier_cv_profile_idx on public.courier_cv (profile_id);

alter table public.courier_cv enable row level security;

-- TEK KATMAN. work_experience iki katman kullanıyor çünkü orada referans
-- bilgisi için bir gizlilik kademesi var; CV'de öyle bir alan yok.
drop policy if exists cv_owner_all on public.courier_cv;
create policy cv_owner_all on public.courier_cv
  for all
  using      (auth.uid() = user_id)
  with check (auth.uid() = user_id);

-- updated_at'i sunucu tutar; istemciye güvenilmez.
create or replace function public.touch_courier_cv()
returns trigger language plpgsql as $$
begin
  new.updated_at := now();
  return new;
end $$;

drop trigger if exists touch_courier_cv_trg on public.courier_cv;
create trigger touch_courier_cv_trg
  before update on public.courier_cv
  for each row execute function public.touch_courier_cv();

-- Okuma yolu. AÇIK KOLON LİSTESİ — sonradan eklenen bir kolon sessizce
-- dışarı sızmasın diye (profiles_public ile aynı gerekçe).
drop view if exists public.courier_cv_public;
create view public.courier_cv_public with (security_invoker = false) as
  select
    c.id, c.profile_id,
    c.ozet, c.ehliyet_sinifi, c.ehliyet_tarihi,
    c.src_belge, c.src_gecerlilik,
    c.egitim, c.tercih_bolgeler, c.musaitlik,
    c.updated_at
  from public.courier_cv c
  join public.profiles p on p.id = c.profile_id
 where c.yayinlandi = true
   and p.yayinda    = true
   and p.role       = 'kurye'
   and p.user_id is not null;

-- Misafire KAPALI — profiles select politikasıyla aynı sınır (migration-20).
grant select on public.courier_cv_public to authenticated;
```

- [ ] **Adım 2: Commit**

```bash
git add supabase/migration-36-kurye-cv.sql
git commit -m "feat(db): courier_cv tablosu, RLS ve yayin gorunumu"
```

- [ ] **Adım 3: Kullanıcıdan çalıştırmasını iste — DUR**

Onay gelmeden Adım 4'e geçme. Ayrıca **iki kez** çalıştırmasını iste
(idempotency kanıtı); ikinci çalıştırma hatasız geçmeli.

- [ ] **Adım 4: Tablo ve görünümün varlığını doğrula**

```bash
printf 'courier_cv        '; curl -s -o /dev/null -w '%{http_code}\n' \
  "$U/courier_cv?select=id&limit=1" -H "apikey: $K" -H "Authorization: Bearer $K"
printf 'courier_cv_public '; curl -s -o /dev/null -w '%{http_code}\n' \
  "$U/courier_cv_public?select=id&limit=1" -H "apikey: $K" -H "Authorization: Bearer $K"
```

Beklenen: `courier_cv` → **401** (RLS var, policy anon'a kapalı),
`courier_cv_public` → **401** (grant yalnız `authenticated`).
**404 / PGRST205 dönerse** migration çalışmamıştır.

- [ ] **Adım 5: Misafirin okuyamadığını doğrula (RLS matrisi 1/4)**

Adım 4'teki iki 401 bu maddeyi karşılar. Kalan üç durum (kurye kendi satırını
yazar / başkasınınkini yazamaz / işveren yayınlanmışı okur) Görev 3'te veri
katmanı hazır olunca tarayıcıda doğrulanır.

- [ ] **Adım 6: Doğrulama sonucunu commit'e ekle**

```bash
git commit --allow-empty -m "test(db): migration-36 dogrulandi - tablo+gorunum var, misafire kapali"
```

---

### Görev 3: `supabase.js` veri katmanı

**Dosyalar:**
- Değiştir: `docs/assets/js/supabase.js` (work experience bloğunun hemen
  ardına, ~1260. satır civarı) ve export nesnesi (~1709)

**Arayüzler:**
- Tüketir: Görev 2'nin `courier_cv` / `courier_cv_public` yapıları
- Üretir:
  - `SB.myCv()` → `Promise<Object|null>` — kendi CV'm (yayınlanmamış olsa da)
  - `SB.cvFor(profileId)` → `Promise<Object|null>` — başkasının yayınlanmış CV'si
  - `SB.saveCv(data)` → `Promise<Object>` — upsert, kaydedilen satırı döner
  - Nesne biçimi: `{ id, profileId, ozet, ehliyetSinifi[], ehliyetTarihi,
    srcBelge, srcGecerlilik, egitim[], tercihBolgeler[], musaitlik,
    yayinlandi, updatedAt }`

- [ ] **Adım 1: Dönüştürücü ve eksik-tablo korumasını yaz**

`_weEksikMi` deseninin aynısı. `docs/assets/js/supabase.js` içine,
`deleteWorkExperience` fonksiyonunun hemen ardına ekle:

```js
  /* ── Kurye özgeçmişi (migration-36) ───────────────────────── */
  /* migration-36 çalıştırılmadıysa tablo/görünüm yoktur. _weYok
     deseniyle aynı: bir kez işaretle, bir daha isteme. */
  var _cvYok = false;
  function _cvEksikMi(err) {
    if (!err) return false;
    var m = (err.message || '') + ' ' + (err.code || '');
    return /Could not find the table|does not exist|42P01|PGRST205/i.test(m);
  }

  function cvFromDb(r) {
    if (!r) return null;
    return {
      id:             r.id,
      profileId:      r.profile_id,
      ozet:           r.ozet || '',
      ehliyetSinifi:  r.ehliyet_sinifi || [],
      ehliyetTarihi:  r.ehliyet_tarihi || null,
      srcBelge:       !!r.src_belge,
      srcGecerlilik:  r.src_gecerlilik || null,
      egitim:         Array.isArray(r.egitim) ? r.egitim : [],
      tercihBolgeler: r.tercih_bolgeler || [],
      musaitlik:      r.musaitlik || '',
      yayinlandi:     !!r.yayinlandi,
      updatedAt:      r.updated_at || null
    };
  }

  function _cvRow(d) {
    return {
      ozet:            (d.ozet || '').slice(0, 600),
      ehliyet_sinifi:  d.ehliyetSinifi || [],
      ehliyet_tarihi:  d.ehliyetTarihi || null,
      src_belge:       !!d.srcBelge,
      src_gecerlilik:  d.srcGecerlilik || null,
      egitim:          Array.isArray(d.egitim) ? d.egitim : [],
      tercih_bolgeler: d.tercihBolgeler || [],
      musaitlik:       d.musaitlik || '',
      yayinlandi:      !!d.yayinlandi
    };
  }
```

- [ ] **Adım 2: Üç fonksiyonu yaz**

```js
  async function myCv() {
    if (_cvYok) return null;
    var u = await getUser(); if (!u) return null;
    var r = await client.from('courier_cv').select('*').eq('user_id', u.id).maybeSingle();
    if (r.error) {
      if (_cvEksikMi(r.error)) { _cvYok = true; return null; }
      console.warn('myCv:', r.error); return null;
    }
    return cvFromDb(r.data);
  }

  async function cvFor(profileId) {
    if (!profileId || _cvYok) return null;
    var r = await client.from('courier_cv_public').select('*')
      .eq('profile_id', profileId).maybeSingle();
    if (r.error) {
      if (_cvEksikMi(r.error)) { _cvYok = true; return null; }
      console.warn('cvFor:', r.error); return null;
    }
    return cvFromDb(r.data);
  }

  async function saveCv(data) {
    var u = await getUser(); if (!u) throw new Error('oturum yok');
    var pid = await myPid2(); if (!pid) throw new Error('Önce profilini oluştur.');
    var row = Object.assign(_cvRow(data), { profile_id: pid, user_id: u.id });
    var r = await client.from('courier_cv')
      .upsert(row, { onConflict: 'profile_id' }).select().maybeSingle();
    if (r.error) throw r.error;
    return cvFromDb(r.data);
  }
```

`myPid2` zaten dosyada tanımlı (`addWorkExperience` kullanıyor) — yeniden
tanımlama.

- [ ] **Adım 3: Export'a ekle**

`deleteWorkExperience: deleteWorkExperience,` satırının hemen ardına:

```js
    myCv: myCv, cvFor: cvFor, saveCv: saveCv,
```

- [ ] **Adım 4: Sözdizimini denetle**

```bash
node --check docs/assets/js/supabase.js
```

Beklenen: çıktı yok (başarılı).

- [ ] **Adım 5: RLS matrisinin kalan üç maddesini tarayıcıda doğrula**

`npx http-server docs -c-1`, kurye hesabıyla giriş, konsolda:

```js
await SB.saveCv({ ozet: 'test', ehliyetSinifi: ['A2'], srcBelge: true, yayinlandi: true });
await SB.myCv();                 // dolu nesne dönmeli
await SB.cvFor('<baska-kurye-profil-id>');   // null dönmeli (o kişinin CV'si yok)
```

Ardından **başka bir kullanıcının** `user_id`'siyle satır yazmayı dene:

```js
await SB.raw.from('courier_cv').update({ ozet: 'x' }).eq('user_id', '<baska-user-id>');
```

Beklenen: 0 satır etkilenir (RLS engeller).

- [ ] **Adım 6: Commit**

```bash
git add docs/assets/js/supabase.js
git commit -m "feat(cv): myCv/cvFor/saveCv veri katmani"
```

---

### Görev 4: `cv.js` — sihirbaz motoru ve 1/2/3/5. adımlar

**Dosyalar:**
- Oluştur: `docs/assets/js/cv.js`

**Arayüzler:**
- Tüketir: `SB.myCv/saveCv`, `SB.myProfile`, `KB.esc`
- Üretir:
  - `KBCV.ADIMLAR` → adım tanımları dizisi
  - `KBCV.load()` → `Promise` — CV + profil önbelleğe alınır
  - `KBCV.state()` → sihirbazın o anki taslak nesnesi
  - `KBCV.renderStep(n)` → `string` (HTML)
  - `KBCV.collectStep(n)` → taslağı DOM'dan günceller
  - `KBCV.gecerliMi(n)` → `{ok:boolean, mesaj:string}`
  - `KBCV.kaydet(yayinla)` → `Promise<{ok}|{error}>`
  - `KBCV.tamamlanma()` → `0..100` tamsayı

- [ ] **Adım 1: Modül iskeleti ve durum yönetimini yaz**

```js
/* ============================================================
   cv.js — kurye özgeçmişi sihirbazı

   Tasarım: specs/2026-08-19-kurye-cv-design.md

   Veri tek kaynaktan gelir:
     • CV alanları        → public.courier_cv   (SB.myCv / SB.saveCv)
     • İş deneyimi        → public.work_experience (SB.*WorkExperience)
     • Ad/şehir/açıklama  → public.profiles     (SB.myProfile)

   Sihirbaz iş deneyimini KOPYALAMAZ; aynı tabloya yazar. Bu yüzden
   profil-duzenle.html ile bu sayfa arasında veri ayrışması olamaz.
   ============================================================ */
(function () {
  'use strict';

  var EHLIYET = ['A1', 'A2', 'A', 'B'];
  var MUSAITLIK = ['Tam zamanlı', 'Yarı zamanlı', 'Hafta sonu', 'Vardiyalı', 'Esnek'];

  var _cv = null;        // DB'den gelen CV
  var _profil = null;    // DB'den gelen profil
  var _taslak = null;    // sihirbazın üzerinde çalıştığı nesne
  var _yuklendi = false;

  var TASLAK_KEY = 'kb_draft:cv';

  function esc(s) {
    return (window.KB && KB.esc) ? KB.esc(s) : String(s == null ? '' : s);
  }
  function on() { return !!(window.SB && SB.isOn && SB.isOn()); }

  function bosTaslak() {
    return {
      ozet: '', ehliyetSinifi: [], ehliyetTarihi: null,
      srcBelge: false, srcGecerlilik: null,
      egitim: [], tercihBolgeler: [], musaitlik: '',
      yayinlandi: false
    };
  }
```

- [ ] **Adım 2: Yükleme ve taslak kalıcılığını yaz**

```js
  async function load() {
    if (_yuklendi) return _taslak;
    if (!on()) { _taslak = bosTaslak(); _yuklendi = true; return _taslak; }

    try { _profil = await SB.myProfile(); } catch (e) { _profil = null; }
    try { _cv = await SB.myCv(); } catch (e) { _cv = null; }

    _taslak = _cv ? JSON.parse(JSON.stringify(_cv)) : bosTaslak();

    /* Melez doldurma: özet boşsa profildeki açıklamadan gelir.
       Kullanıcı bir kez düzenlerse artık ona dokunulmaz. */
    if (!_taslak.ozet && _profil && _profil.aciklama) _taslak.ozet = _profil.aciklama;
    if (!_taslak.tercihBolgeler.length && _profil && _profil.bolgeler) {
      _taslak.tercihBolgeler = _profil.bolgeler.slice();
    }

    /* Yarım kalmış oturum taslağı varsa onu tercih et. */
    try {
      var ham = sessionStorage.getItem(TASLAK_KEY);
      if (ham) _taslak = Object.assign(_taslak, JSON.parse(ham));
    } catch (e) {}

    _yuklendi = true;
    return _taslak;
  }

  function taslagiYaz() {
    try { sessionStorage.setItem(TASLAK_KEY, JSON.stringify(_taslak)); } catch (e) {}
  }
  function taslagiSil() {
    try { sessionStorage.removeItem(TASLAK_KEY); } catch (e) {}
  }
```

- [ ] **Adım 3: 1., 2., 3. ve 5. adımların render'ını yaz**

```js
  function stepOzet() {
    return '<label class="cv-label" for="cvOzet">Kendini kısaca anlat</label>' +
      '<textarea id="cvOzet" class="cv-input" rows="5" maxlength="600" ' +
        'placeholder="Örn. 5 yıldır İstanbul Avrupa yakasında motokuryelik yapıyorum…">' +
        esc(_taslak.ozet) + '</textarea>' +
      '<p class="cv-hint">İşverenin ilk okuduğu yer burasıdır. 2-3 cümle yeterli.</p>';
  }

  function stepEhliyet() {
    var h = '<div class="cv-field"><span class="cv-label">Ehliyet sınıfın</span><div class="cv-chips">';
    EHLIYET.forEach(function (s) {
      var sec = _taslak.ehliyetSinifi.indexOf(s) > -1;
      h += '<button type="button" class="cv-chip' + (sec ? ' is-on' : '') +
           '" data-ehliyet="' + esc(s) + '">' + esc(s) + '</button>';
    });
    h += '</div><p class="cv-hint">Ticari motokuryelik için A1, A2 veya A gerekir; ' +
         'B sınıfı otomobil ehliyeti tek başına yeterli değildir.</p></div>';

    h += '<div class="cv-field"><label class="cv-label" for="cvEhliyetTarih">Ehliyet alış tarihi</label>' +
         '<input type="date" id="cvEhliyetTarih" class="cv-input" value="' +
         esc(_taslak.ehliyetTarihi || '') + '"></div>';

    h += '<div class="cv-field"><label class="cv-check">' +
         '<input type="checkbox" id="cvSrc"' + (_taslak.srcBelge ? ' checked' : '') + '> ' +
         '<span>SRC Kurye Belgem var</span></label>' +
         '<p class="cv-hint">15 Mayıs 2026\'dan bu yana motokuryeler için zorunludur.</p></div>';

    h += '<div class="cv-field" id="cvSrcTarihWrap"' +
         (_taslak.srcBelge ? '' : ' style="display:none"') + '>' +
         '<label class="cv-label" for="cvSrcGecerlilik">SRC geçerlilik tarihi</label>' +
         '<input type="date" id="cvSrcGecerlilik" class="cv-input" value="' +
         esc(_taslak.srcGecerlilik || '') + '"></div>';

    h += '<p class="cv-note">Adli sicil, sağlık raporu ve psikoteknik burada ' +
         'saklanmaz — işveren görüşmede talep edebilir.</p>';
    return h;
  }

  function stepEgitim() {
    var h = '<div id="cvEgitimListe">';
    if (!_taslak.egitim.length) {
      h += '<p class="cv-empty">Henüz eğitim bilgisi eklenmedi.</p>';
    } else {
      _taslak.egitim.forEach(function (e, i) {
        h += '<div class="cv-row"><div class="cv-row__main">' +
             '<b>' + esc(e.okul || '—') + '</b>' +
             '<span>' + esc(e.derece || '') + (e.yil ? ' · ' + esc(e.yil) : '') + '</span>' +
             '</div><button type="button" class="btn btn--ghost btn--sm" ' +
             'data-egitim-sil="' + i + '">Sil</button></div>';
      });
    }
    h += '</div><div class="cv-inline">' +
      '<input type="text" id="cvOkul"   class="cv-input" placeholder="Okul adı" maxlength="90">' +
      '<input type="text" id="cvDerece" class="cv-input" placeholder="Derece (Lise, Ön lisans…)" maxlength="60">' +
      '<input type="number" id="cvYil"  class="cv-input" placeholder="Yıl" min="1950" max="2100">' +
      '<button type="button" class="btn btn--primary btn--sm" id="cvEgitimEkle">Ekle</button>' +
      '</div>';
    return h;
  }

  function stepTercih() {
    var h = '<div class="cv-field"><label class="cv-label" for="cvBolgeler">' +
      'Çalışmak istediğin bölgeler</label>' +
      '<input type="text" id="cvBolgeler" class="cv-input" ' +
      'placeholder="Kadıköy, Ataşehir, Ümraniye" value="' +
      esc(_taslak.tercihBolgeler.join(', ')) + '">' +
      '<p class="cv-hint">Virgülle ayır.</p></div>';

    h += '<div class="cv-field"><span class="cv-label">Müsaitlik</span><div class="cv-chips">';
    MUSAITLIK.forEach(function (m) {
      h += '<button type="button" class="cv-chip' +
           (_taslak.musaitlik === m ? ' is-on' : '') +
           '" data-musaitlik="' + esc(m) + '">' + esc(m) + '</button>';
    });
    return h + '</div></div>';
  }
```

- [ ] **Adım 4: Toplama ve doğrulamayı yaz**

```js
  function val(id) {
    var el = document.getElementById(id);
    return el ? el.value : '';
  }

  function collectStep(n) {
    if (n === 1) _taslak.ozet = val('cvOzet').trim();
    if (n === 2) {
      _taslak.ehliyetTarihi = val('cvEhliyetTarih') || null;
      var src = document.getElementById('cvSrc');
      _taslak.srcBelge = !!(src && src.checked);
      _taslak.srcGecerlilik = _taslak.srcBelge ? (val('cvSrcGecerlilik') || null) : null;
    }
    if (n === 5) {
      _taslak.tercihBolgeler = val('cvBolgeler').split(',')
        .map(function (s) { return s.trim(); })
        .filter(function (s) { return s.length > 0; });
    }
    taslagiYaz();
  }

  /* Doğrulama gerçekten engelleyici olmalı; "tüm alanları doldurun" deyip
     tek alanla geçiren sahte kontrol YAZILMAZ. */
  function gecerliMi(n) {
    if (n === 1 && _taslak.ozet.trim().length < 20) {
      return { ok: false, mesaj: 'Özet en az 20 karakter olmalı.' };
    }
    if (n === 2 && !_taslak.ehliyetSinifi.length) {
      return { ok: false, mesaj: 'En az bir ehliyet sınıfı seç.' };
    }
    if (n === 2 && _taslak.srcBelge && !_taslak.srcGecerlilik) {
      return { ok: false, mesaj: 'SRC belgesi işaretliyse geçerlilik tarihi gerekir.' };
    }
    return { ok: true, mesaj: '' };
  }

  function tamamlanma() {
    var puan = 0, toplam = 6;
    if (_taslak.ozet.trim().length >= 20) puan++;
    if (_taslak.ehliyetSinifi.length)     puan++;
    if (_taslak.srcBelge)                 puan++;
    if (_taslak.egitim.length)            puan++;
    if (_taslak.tercihBolgeler.length)    puan++;
    if (_taslak.musaitlik)                puan++;
    return Math.round(puan / toplam * 100);
  }
```

- [ ] **Adım 5: Kaydetme ve export'u yaz**

```js
  async function kaydet(yayinla) {
    if (!on()) return { error: 'Sunucuya bağlanılamadı — kaydedilmedi.' };
    _taslak.yayinlandi = !!yayinla;
    try {
      var kayit = await SB.saveCv(_taslak);
      _cv = kayit;
      taslagiSil();
      return { ok: true, cv: kayit };
    } catch (e) {
      return { error: (e && e.message) || 'Özgeçmiş kaydedilemedi.' };
    }
  }

  window.KBCV = {
    EHLIYET: EHLIYET,
    MUSAITLIK: MUSAITLIK,
    load: load,
    state: function () { return _taslak; },
    profil: function () { return _profil; },
    renderStep: function (n) {
      if (n === 1) return stepOzet();
      if (n === 2) return stepEhliyet();
      if (n === 3) return stepEgitim();
      if (n === 5) return stepTercih();
      return '';
    },
    collectStep: collectStep,
    gecerliMi: gecerliMi,
    tamamlanma: tamamlanma,
    kaydet: kaydet
  };
})();
```

- [ ] **Adım 6: Sözdizimini denetle ve ev stilini doğrula**

```bash
node --check docs/assets/js/cv.js
grep -nE "=>|^\s*(const|let) " docs/assets/js/cv.js
```

Beklenen: `node --check` sessiz; `grep` **hiçbir şey bulmamalı** (ev stili).

- [ ] **Adım 7: Commit**

```bash
git add docs/assets/js/cv.js
git commit -m "feat(cv): sihirbaz motoru - ozet, ehliyet, egitim, tercih adimlari"
```

---

### Görev 5: 4. adım — iş deneyimi tam düzenleme

Kullanıcının kararı: tek sihirbazda her şey. Ama veri kopyalanmaz.

**Dosyalar:**
- Değiştir: `docs/assets/js/cv.js`

**Arayüzler:**
- Tüketir: `SB.myWorkExperience`, `SB.addWorkExperience`,
  `SB.updateWorkExperience`, `SB.deleteWorkExperience` (hepsi mevcut)
- Üretir: `KBCV.renderStep(4)`, `KBCV.deneyimYukle()`, `KBCV.deneyimKaydet()`,
  `KBCV.deneyimSil(id)`

- [ ] **Adım 1: Deneyim önbelleği ve render'ı yaz**

`cv.js` içinde, `stepTercih`'in ardına:

```js
  var _deneyim = [];

  async function deneyimYukle() {
    if (!on() || !SB.myWorkExperience) { _deneyim = []; return _deneyim; }
    try { _deneyim = (await SB.myWorkExperience()) || []; }
    catch (e) { console.warn('deneyimYukle:', e); _deneyim = []; }
    return _deneyim;
  }

  function weTarih(d) {
    if (!d) return '';
    var p = String(d).split('-');
    return p.length >= 2 ? p[1] + '.' + p[0] : String(d);
  }

  function stepDeneyim() {
    var h = '<div id="cvDeneyimListe">';
    if (!_deneyim.length) {
      h += '<p class="cv-empty">Henüz iş deneyimi eklenmedi.</p>';
    } else {
      _deneyim.forEach(function (d) {
        var bit = d.aktif ? 'Devam ediyor' : weTarih(d.bitis);
        h += '<div class="cv-row"><div class="cv-row__main">' +
             '<b>' + esc(d.pozisyon || '—') + '</b>' +
             '<span>' + esc(d.sirket || '') + ' · ' +
             esc(weTarih(d.baslangic)) + ' – ' + esc(bit) + '</span>' +
             '</div><button type="button" class="btn btn--ghost btn--sm" ' +
             'data-deneyim-sil="' + esc(d.id) + '">Sil</button></div>';
      });
    }
    h += '</div><div class="cv-subform">' +
      '<input type="text" id="cvExpSirket"   class="cv-input" placeholder="Şirket adı *" maxlength="80">' +
      '<input type="text" id="cvExpPozisyon" class="cv-input" placeholder="Pozisyon *" maxlength="80">' +
      '<input type="text" id="cvExpSehir"    class="cv-input" placeholder="Şehir" maxlength="60">' +
      '<div class="cv-inline">' +
        '<input type="month" id="cvExpBas" class="cv-input">' +
        '<input type="month" id="cvExpBit" class="cv-input">' +
        '<label class="cv-check"><input type="checkbox" id="cvExpAktif"> <span>Devam ediyor</span></label>' +
      '</div>' +
      '<textarea id="cvExpAciklama" class="cv-input" rows="2" maxlength="400" ' +
        'placeholder="Ne yaptın? (isteğe bağlı)"></textarea>' +
      '<button type="button" class="btn btn--primary btn--sm" id="cvDeneyimEkle">Deneyim Ekle</button>' +
      '</div>';
    return h;
  }
```

- [ ] **Adım 2: Ekleme ve silmeyi yaz**

```js
  /* AYNI TABLOYA yazar: public.work_experience. Kopya tutulmaz, bu
     yüzden profil-duzenle.html ile ayrışma imkânsızdır. */
  async function deneyimKaydet() {
    var sirket   = (val('cvExpSirket')   || '').trim();
    var pozisyon = (val('cvExpPozisyon') || '').trim();
    if (!sirket || !pozisyon) return { error: 'Şirket ve pozisyon zorunlu.' };

    var aktifEl = document.getElementById('cvExpAktif');
    var aktif   = !!(aktifEl && aktifEl.checked);
    var bas     = val('cvExpBas');
    var bit     = val('cvExpBit');

    try {
      await SB.addWorkExperience({
        sirket: sirket, pozisyon: pozisyon,
        sehir: (val('cvExpSehir') || '').trim(),
        baslangic: bas ? bas + '-01' : null,
        bitis: (!aktif && bit) ? bit + '-01' : null,
        aktif: aktif,
        aciklama: (val('cvExpAciklama') || '').trim()
      });
      await deneyimYukle();
      return { ok: true };
    } catch (e) {
      return { error: (e && e.message) || 'Deneyim eklenemedi.' };
    }
  }

  async function deneyimSil(id) {
    try {
      await SB.deleteWorkExperience(id);
      await deneyimYukle();
      return { ok: true };
    } catch (e) {
      return { error: (e && e.message) || 'Deneyim silinemedi.' };
    }
  }
```

- [ ] **Adım 3: Export'u genişlet**

`renderStep` içine `if (n === 4) return stepDeneyim();` ekle ve export
nesnesine şunları ekle:

```js
    deneyimYukle: deneyimYukle,
    deneyimListe: function () { return _deneyim; },
    deneyimKaydet: deneyimKaydet,
    deneyimSil: deneyimSil,
```

- [ ] **Adım 4: Sözdizimi ve stil denetimi**

```bash
node --check docs/assets/js/cv.js
grep -nE "=>|^\s*(const|let) " docs/assets/js/cv.js
```

Beklenen: ikisi de sessiz.

- [ ] **Adım 5: Commit**

```bash
git add docs/assets/js/cv.js
git commit -m "feat(cv): 4. adim - is deneyimi ayni tablo uzerinde tam duzenleme"
```

---

### Görev 6: `cv-olustur.html` — sihirbaz sayfası

**Dosyalar:**
- Oluştur: `docs/cv-olustur.html`
- Değiştir: `docs/assets/css/main.css` (dosya sonuna `cv-*` blokları)

**Arayüzler:**
- Tüketir: `KBCV.*`, `KB.*`, `KBMotion.*`
- Üretir: kullanıcıya görünen sihirbaz; `?adim=N` ile derin bağlantı

- [ ] **Adım 1: Sayfa kabuğunu yaz**

Script sırası CLAUDE.md'deki zorunlu sırayla birebir aynı olmalı; `cv.js`
`app.js`'ten **sonra** gelir (KB'ye bağımlı):

```html
<!DOCTYPE html>
<html lang="tr" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <title>Özgeçmişimi Oluştur · KuryemiBul</title>
  <link rel="stylesheet" href="assets/css/main.css">
</head>
<body>
  <div id="app-header"></div>
  <div id="app-topbar"></div>
  <div id="sidebar-overlay"></div>
  <nav id="app-sidebar"></nav>

  <div class="app-content">
    <div class="cv-wrap" id="cvRoot">
      <div class="cv-head">
        <h1 class="cv-title">Özgeçmişini Oluştur</h1>
        <div class="cv-progress"><div class="cv-progress__fill" id="cvProgress"></div></div>
        <div class="cv-steps" id="cvSteps"></div>
      </div>
      <div class="cv-body" id="cvBody"></div>
      <div class="cv-foot">
        <button type="button" class="btn btn--ghost" id="cvGeri">Geri</button>
        <button type="button" class="btn btn--primary" id="cvIleri">İleri</button>
      </div>
    </div>
  </div>

  <div id="app-footer"></div>

  <script src="assets/js/i18n.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.112.2/dist/umd/supabase.js" integrity="sha384-OUpie84zd1LdwNlK9uJJQRwab0BLqo3eKYKFh7hSVL58FSk7wPp2l0kfUMIIoaQd" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
  <script src="assets/js/supabase.js"></script>
  <script src="assets/js/presence.js"></script>
  <script src="assets/js/components.js"></script>
  <script src="assets/js/app.js"></script>
  <script src="assets/js/cv.js"></script>
  <script src="assets/js/motion.js"></script>
  <script>
  /* … Adım 2'deki sihirbaz sürücüsü … */
  </script>
</body>
</html>
```

- [ ] **Adım 2: Sihirbaz sürücüsünü yaz**

Rol kapısı burada: sayfa yalnız kuryeye açıktır.

```js
  (function () {
    'use strict';
    var ADIMLAR = ['Özet', 'Ehliyet & Belge', 'Eğitim', 'Deneyim', 'Tercihler', 'Önizleme'];
    var adim = 1;

    function E(s) { return (window.KB && KB.esc) ? KB.esc(s) : String(s == null ? '' : s); }
    function hata(m) {
      if (window.KBMotion && KBMotion.showErrorToast) KBMotion.showErrorToast(m);
      else alert(m);
    }

    function cizAdimlar() {
      document.getElementById('cvSteps').innerHTML = ADIMLAR.map(function (ad, i) {
        var n = i + 1;
        return '<button type="button" class="cv-step' + (n === adim ? ' is-on' : '') +
               (n < adim ? ' is-done' : '') + '" data-adim="' + n + '">' +
               n + '. ' + E(ad) + '</button>';
      }).join('');
      document.getElementById('cvProgress').style.width = KBCV.tamamlanma() + '%';
    }

    async function ciz() {
      var body = document.getElementById('cvBody');
      if (adim === 4) await KBCV.deneyimYukle();
      body.innerHTML = (adim === 6) ? onizleme() : KBCV.renderStep(adim);
      document.getElementById('cvIleri').textContent = (adim === 6) ? 'Yayınla' : 'İleri';
      document.getElementById('cvGeri').style.visibility = (adim === 1) ? 'hidden' : 'visible';
      cizAdimlar();
    }

    function onizleme() {
      var s = KBCV.state();
      var h = '<div class="cv-preview">';
      h += '<p class="cv-hint">İşveren profilinde bunu görecek.</p>';
      if (s.ozet) h += '<p class="cv-preview__ozet">' + E(s.ozet) + '</p>';
      if (s.ehliyetSinifi.length) {
        h += '<div class="cv-chips">' + s.ehliyetSinifi.map(function (x) {
          return '<span class="chip">' + E(x) + ' ehliyet</span>';
        }).join('') + '</div>';
      }
      if (s.srcBelge) h += '<p><span class="chip chip--ok">SRC Kurye Belgesi ✓</span></p>';
      if (!s.ozet && !s.ehliyetSinifi.length) {
        h += '<p class="cv-empty">Henüz doldurulmuş bir alan yok.</p>';
      }
      return h + '</div>';
    }

    document.getElementById('cvIleri').addEventListener('click', async function () {
      KBCV.collectStep(adim);
      var g = KBCV.gecerliMi(adim);
      if (!g.ok) { hata(g.mesaj); return; }
      if (adim < 6) { adim++; await ciz(); return; }
      var r = await KBCV.kaydet(true);
      if (r.error) { hata(r.error); return; }
      if (window.KBMotion && KBMotion.showSuccess) {
        KBMotion.showSuccess('Özgeçmişin yayınlandı', 'İşverenler artık görebilir', 2200);
      }
      setTimeout(function () { location.href = 'profil-kurye.html'; }, 1200);
    });

    document.getElementById('cvGeri').addEventListener('click', async function () {
      KBCV.collectStep(adim);
      if (adim > 1) { adim--; await ciz(); }
    });

    /* Çip ve satır tıklamaları — tek delege dinleyici */
    document.getElementById('cvBody').addEventListener('click', async function (e) {
      var t = e.target.closest('[data-ehliyet],[data-musaitlik],[data-egitim-sil],[data-deneyim-sil]');
      if (!t) return;
      var s = KBCV.state();
      if (t.hasAttribute('data-ehliyet')) {
        var v = t.getAttribute('data-ehliyet');
        var i = s.ehliyetSinifi.indexOf(v);
        if (i > -1) s.ehliyetSinifi.splice(i, 1); else s.ehliyetSinifi.push(v);
        await ciz(); return;
      }
      if (t.hasAttribute('data-musaitlik')) {
        s.musaitlik = t.getAttribute('data-musaitlik');
        await ciz(); return;
      }
      if (t.hasAttribute('data-egitim-sil')) {
        s.egitim.splice(parseInt(t.getAttribute('data-egitim-sil'), 10), 1);
        await ciz(); return;
      }
      if (t.hasAttribute('data-deneyim-sil')) {
        var r = await KBCV.deneyimSil(t.getAttribute('data-deneyim-sil'));
        if (r.error) hata(r.error);
        await ciz();
      }
    });

    document.getElementById('cvBody').addEventListener('change', function (e) {
      if (e.target && e.target.id === 'cvSrc') {
        var w = document.getElementById('cvSrcTarihWrap');
        if (w) w.style.display = e.target.checked ? '' : 'none';
      }
    });

    /* Ekle düğmeleri */
    document.getElementById('cvBody').addEventListener('click', async function (e) {
      if (e.target && e.target.id === 'cvEgitimEkle') {
        var okul = (document.getElementById('cvOkul').value || '').trim();
        if (!okul) { hata('Okul adı gerekli.'); return; }
        KBCV.state().egitim.push({
          okul: okul,
          derece: (document.getElementById('cvDerece').value || '').trim(),
          yil: (document.getElementById('cvYil').value || '').trim()
        });
        await ciz();
      }
      if (e.target && e.target.id === 'cvDeneyimEkle') {
        var r = await KBCV.deneyimKaydet();
        if (r.error) hata(r.error);
        await ciz();
      }
    });

    /* Başlat — YALNIZ KURYE. Diğer roller kendi paneline gider. */
    KB.ready().then(async function () {
      var rol = KB.getRole ? KB.getRole() : 'guest';
      if (rol !== 'kurye') {
        location.replace(rol === 'firma' ? 'panel-firma.html'
                       : rol === 'isletme' ? 'panel-isletme.html' : 'index.html');
        return;
      }
      await KBCV.load();
      var q = parseInt(new URLSearchParams(location.search).get('adim') || '1', 10);
      adim = (q >= 1 && q <= 6) ? q : 1;
      await ciz();
    });
  })();
```

- [ ] **Adım 3: CSS'i yaz**

`docs/assets/css/main.css` sonuna. Token kullan, sabit renk yazma; koyu tema
otomatik çalışsın:

```css
/* ── CV sihirbazı ─────────────────────────────────────────── */
.cv-wrap { max-width: 780px; margin: 0 auto; padding: 20px 16px 60px; }
.cv-title { font-size: 1.5rem; font-weight: 800; margin: 0 0 12px; }
.cv-progress { height: 6px; background: var(--surface-2); border-radius: 99px; overflow: hidden; }
.cv-progress__fill { height: 100%; background: var(--primary); transition: width .3s ease; }
.cv-steps { display: flex; flex-wrap: wrap; gap: 6px; margin: 14px 0 20px; }
.cv-step { padding: 6px 10px; border-radius: 99px; border: 1px solid var(--border);
           background: transparent; color: var(--text-2); font-size: .78rem; cursor: pointer; }
.cv-step.is-on   { background: var(--primary); color: #fff; border-color: var(--primary); }
.cv-step.is-done { color: var(--success); border-color: var(--success); }
.cv-field { margin-bottom: 18px; }
.cv-label { display: block; font-weight: 600; font-size: .88rem; margin-bottom: 6px; }
.cv-input { width: 100%; padding: 10px 12px; border: 1px solid var(--border);
            border-radius: 10px; background: var(--surface); color: var(--text-1);
            font: inherit; }
.cv-hint { font-size: .78rem; color: var(--text-3); margin: 6px 0 0; }
.cv-note { font-size: .78rem; color: var(--text-3); border-left: 3px solid var(--border);
           padding-left: 10px; margin-top: 18px; }
.cv-empty { font-size: .85rem; color: var(--text-3); padding: 10px 0; }
.cv-chips { display: flex; flex-wrap: wrap; gap: 8px; }
.cv-chip { padding: 7px 14px; border-radius: 99px; border: 1px solid var(--border);
           background: transparent; color: var(--text-2); cursor: pointer; font-size: .84rem; }
.cv-chip.is-on { background: var(--primary); color: #fff; border-color: var(--primary); }
.cv-check { display: flex; align-items: center; gap: 8px; font-size: .88rem; }
.cv-row { display: flex; align-items: center; justify-content: space-between; gap: 10px;
          padding: 10px 0; border-bottom: 1px solid var(--border); }
.cv-row__main { display: flex; flex-direction: column; min-width: 0; }
.cv-row__main span { font-size: .8rem; color: var(--text-3); }
.cv-inline { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 12px; }
.cv-inline .cv-input { flex: 1; min-width: 120px; }
.cv-subform { display: flex; flex-direction: column; gap: 8px; margin-top: 14px; }
.cv-foot { display: flex; justify-content: space-between; gap: 10px; margin-top: 26px; }
.cv-preview__ozet { font-size: .92rem; line-height: 1.6; }
@media (max-width: 680px) {
  .cv-steps { overflow-x: auto; flex-wrap: nowrap; }
  .cv-step { white-space: nowrap; }
}
```

- [ ] **Adım 4: Gömülü script'in sözdizimini denetle**

```bash
node -e "var fs=require('fs');var h=fs.readFileSync('docs/cv-olustur.html','utf8');var m=h.match(/<script(?![^>]*src=)[^>]*>([\s\S]*?)<\/script>/);new (require('vm').Script)(m[1]);console.log('OK')"
```

Beklenen: `OK`.

- [ ] **Adım 5: Rol kapısını doğrula**

`npx http-server docs -c-1`. Firma hesabıyla (`oguzhanyar178@gmail.com`)
`cv-olustur.html`'i aç. Beklenen: `panel-firma.html`'e yönlenir. Kurye
hesabıyla açınca sihirbaz görünür.

- [ ] **Adım 6: Commit**

```bash
git add docs/cv-olustur.html docs/assets/css/main.css
git commit -m "feat(cv): sihirbaz sayfasi, alti adim ve rol kapisi"
```

---

### Görev 7: İşveren tarafı — profile "Özgeçmiş" bölümü

**Dosyalar:**
- Değiştir: `docs/assets/js/app.js` — kurye dalı, `İş Deneyimi` bölümünün
  hemen ardı (~1506. satır)

**Arayüzler:**
- Tüketir: `SB.cvFor(profileId)`
- Üretir: `profil-kurye.html?id=` üzerinde "Özgeçmiş" bölümü

- [ ] **Adım 1: Render yardımcısını yaz**

`app.js` içinde, `renderWorkExperience` fonksiyonunun yanına:

```js
  /* Kurye özgeçmişi (migration-36). Kayıt yoksa veya yayınlanmamışsa
     BÖLÜM HİÇ BASILMAZ — "CV yok" kutusu da göstermeyiz. */
  function renderCv(cv) {
    if (!cv) return '';
    var h = '';
    if (cv.ozet) h += '<p class="prf-cv-ozet">' + KB.esc(cv.ozet) + '</p>';

    var rozet = [];
    (cv.ehliyetSinifi || []).forEach(function (s) {
      rozet.push('<span class="chip">' + KB.esc(s) + ' ehliyet</span>');
    });
    if (cv.srcBelge) rozet.push('<span class="chip chip--ok">SRC Kurye Belgesi</span>');
    if (rozet.length) h += '<div class="prf-chips">' + rozet.join('') + '</div>';

    if (cv.egitim && cv.egitim.length) {
      h += '<div class="prf-cv-blok"><div class="prf-cv-blok__t">Eğitim</div>';
      cv.egitim.forEach(function (e) {
        h += '<div class="prf-cv-satir"><b>' + KB.esc(e.okul || '—') + '</b>' +
             '<span>' + KB.esc(e.derece || '') +
             (e.yil ? ' · ' + KB.esc(e.yil) : '') + '</span></div>';
      });
      h += '</div>';
    }

    if (cv.tercihBolgeler && cv.tercihBolgeler.length) {
      h += '<div class="prf-cv-blok"><div class="prf-cv-blok__t">Çalışmak istediği bölgeler</div>' +
           '<div class="prf-chips">' + cv.tercihBolgeler.map(function (b) {
             return '<span class="chip">' + KB.esc(b) + '</span>';
           }).join('') + '</div></div>';
    }
    if (cv.musaitlik) {
      h += '<div class="prf-cv-blok"><div class="prf-cv-blok__t">Müsaitlik</div>' +
           '<p>' + KB.esc(cv.musaitlik) + '</p></div>';
    }
    h += '<p class="prf-cv-not">Adli sicil, sağlık raporu ve psikoteknik ' +
         'platformda tutulmaz — görüşmede talep edebilirsiniz.</p>';
    return h;
  }
```

- [ ] **Adım 2: Bölümü kurye dalına ekle**

`if (weHtml) sections += prfSection("İş Deneyimi", weHtml);` satırının hemen
ardına:

```js
      /* Özgeçmiş — yalnız kurye dalında. Yayınlanmamışsa hiç basılmaz. */
      if (online() && SB.cvFor) {
        try {
          var cvRow = await SB.cvFor(x.id);
          var cvHtml = renderCv(cvRow);
          if (cvHtml) sections += prfSection("Özgeçmiş", cvHtml);
        } catch (e) { console.warn('cvFor:', e); }
      }
```

- [ ] **Adım 3: CSS ekle**

`main.css` sonuna:

```css
.prf-cv-ozet { font-size: .92rem; line-height: 1.65; margin: 0 0 14px; }
.prf-cv-blok { margin-top: 16px; }
.prf-cv-blok__t { font-size: .78rem; font-weight: 700; text-transform: uppercase;
                  letter-spacing: .05em; color: var(--text-3); margin-bottom: 6px; }
.prf-cv-satir { display: flex; flex-direction: column; padding: 6px 0; }
.prf-cv-satir span { font-size: .8rem; color: var(--text-3); }
.prf-cv-not { font-size: .76rem; color: var(--text-3); margin-top: 18px; }
```

- [ ] **Adım 4: Sözdizimi denetimi**

```bash
node --check docs/assets/js/app.js
```

- [ ] **Adım 5: Boş durumu doğrula**

CV'si olmayan bir kurye profilini aç (`profil-kurye.html?id=<id>`).
Beklenen: "Özgeçmiş" başlığı **hiç görünmüyor** — boş kutu da yok.

- [ ] **Adım 6: Commit**

```bash
git add docs/assets/js/app.js docs/assets/css/main.css
git commit -m "feat(cv): kurye profiline Ozgecmis bolumu"
```

---

### Görev 8: Giriş noktaları — panel kartı ve aday çipleri

**Dosyalar:**
- Değiştir: `docs/panel-kurye.html` (gösterge paneline kart)
- Değiştir: `docs/basvurular.html` (aday kartına çipler)
- Değiştir: `docs/assets/js/i18n.js` (gezinme metinleri)
- Değiştir: `docs/sitemap.xml` (yalnız yorum satırı), `CLAUDE.md`

**Arayüzler:**
- Tüketir: `SB.myCv`, `SB.cvFor`

- [ ] **Adım 1: `panel-kurye.html`'e giriş kartı**

Gösterge paneli bölümüne, mevcut kart kalıbıyla uyumlu:

```js
    async function cizCvKart() {
      var el = document.getElementById('cvPromo');
      if (!el || !(window.SB && SB.isOn() && SB.myCv)) return;
      var cv = null;
      try { cv = await SB.myCv(); } catch (e) { return; }
      if (cv && cv.yayinlandi) {
        el.innerHTML = '<div class="ilds-applied-item">' +
          '<div class="ilds-applied-ico">📄</div>' +
          '<div class="ilds-applied-body"><div class="ilds-applied-title">Özgeçmişin yayında</div>' +
          '<div class="ilds-applied-meta"><span>İşverenler profilinde görebiliyor</span></div></div>' +
          '<a href="cv-olustur.html" class="btn btn--ghost btn--sm">Düzenle</a></div>';
      } else {
        el.innerHTML = '<div class="ilds-applied-item">' +
          '<div class="ilds-applied-ico">📄</div>' +
          '<div class="ilds-applied-body"><div class="ilds-applied-title">Özgeçmişini oluştur</div>' +
          '<div class="ilds-applied-meta"><span>İşverenler başvurunda görsün</span></div></div>' +
          '<a href="cv-olustur.html" class="btn btn--primary btn--sm">Başla</a></div>';
      }
    }
```

`<div id="cvPromo"></div>` yerleştir ve `cizCvKart()`'ı sayfa başlatma
zincirine ekle.

- [ ] **Adım 2: `basvurular.html`'e SRC / ehliyet çipleri**

Aday kartı render'ında, `k.arac` çipinin yanına. `cvFor` başvuru başına bir
istek attığı için sonuçlar bir haritada önbelleklenir:

```js
      var _cvOnbellek = {};
      async function cvCipleri(profilId) {
        if (!(window.SB && SB.isOn() && SB.cvFor)) return '';
        if (_cvOnbellek[profilId] !== undefined) return _cvOnbellek[profilId];
        var h = '';
        try {
          var cv = await SB.cvFor(profilId);
          if (cv) {
            if (cv.srcBelge) h += '<span class="chip chip--ok">SRC ✓</span>';
            (cv.ehliyetSinifi || []).forEach(function (s) {
              h += '<span class="chip">' + esc(s) + ' ehliyet</span>';
            });
          }
        } catch (e) {}
        _cvOnbellek[profilId] = h;
        return h;
      }
```

- [ ] **Adım 3: `sitemap.xml`'e EKLEME — gerekçeyi doğrula**

`docs/sitemap.xml` bilinçli olarak yalnız **girişsiz taranabilir** sayfaları
listeliyor (dosyanın başındaki yorum bloğu bunu açıkça söylüyor: havuz ve ilan
sayfaları giriş gerektirdiği için dışarıda). `cv-olustur.html` kurye girişi
ister, dolayısıyla **sitemap'e eklenmez.**

Yapılacak tek şey: dosyanın başındaki yorum bloğuna bir satır eklemek, ki
gelecekte biri "neden yok" diye sormasın:

```xml
  cv-olustur.html YOK: kurye girişi gerektiriyor, taranabilir değil.
```

- [ ] **Adım 4: `CLAUDE.md`'ye yeni yüzeyi yaz**

İki yere dokunulur. "Global Nesneler" listesine:

```markdown
- `window.KBCV` — `cv.js` tarafından export edilir. Kurye özgeçmişi sihirbazı:
  `KBCV.load()`, `KBCV.renderStep(n)`, `KBCV.collectStep(n)`, `KBCV.gecerliMi(n)`,
  `KBCV.kaydet(yayinla)`, `KBCV.tamamlanma()`. İş deneyimi adımı veriyi
  kopyalamaz — `SB.*WorkExperience` ile aynı `work_experience` tablosuna yazar.
```

"Sayfa Kategorileri" listesine, **Yardımcı** satırının ardına:

```markdown
- **Özgeçmiş:** `cv-olustur.html` — altı adımlı CV sihirbazı. **Yalnız kurye
  rolüne açıktır**; diğer roller kendi paneline yönlendirilir. Çıktı
  `profil-kurye.html` üzerindeki "Özgeçmiş" bölümüdür; `courier_cv.yayinlandi`
  false ise bölüm hiç basılmaz.
```

- [ ] **Adım 5: i18n — gezinme metinleri sözlüğe girer**

CLAUDE.md kuralı kullanıcıya görünen metinlerin `KBI18N.t()` ile çekilmesini
söylüyor. Yeni sayfada bunu **gezinme düzeyinde** uygula: sayfa başlığı, altı
adım adı ve iki düğme. `docs/assets/js/i18n.js` içindeki `DICT`'e her iki dil
için ekle:

```js
  "cv.title":    { tr: "Özgeçmişini Oluştur",  en: "Build Your CV" },
  "cv.s1":       { tr: "Özet",                 en: "Summary" },
  "cv.s2":       { tr: "Ehliyet & Belge",      en: "Licence & Docs" },
  "cv.s3":       { tr: "Eğitim",               en: "Education" },
  "cv.s4":       { tr: "Deneyim",              en: "Experience" },
  "cv.s5":       { tr: "Tercihler",            en: "Preferences" },
  "cv.s6":       { tr: "Önizleme",             en: "Preview" },
  "cv.next":     { tr: "İleri",                en: "Next" },
  "cv.back":     { tr: "Geri",                 en: "Back" },
  "cv.publish":  { tr: "Yayınla",              en: "Publish" },
  "cv.section":  { tr: "Özgeçmiş",             en: "CV" },
```

Sonra `cv-olustur.html` içindeki `ADIMLAR` dizisini ve düğme metinlerini
`KBI18N.t()`'den okuyacak şekilde değiştir; `app.js`'teki
`prfSection("Özgeçmiş", …)` çağrısını `prfSection(T("cv.section"), …)` yap.

**Bilinen sapma — kayda geçirilir:** sihirbaz içindeki yardım/ipucu metinleri
(`cv-hint`, `cv-note`, placeholder'lar) Türkçe sabit kalır. Gerekçe: komşu
sayfalar (`panel-kurye.html`, `basvurular.html`) de aynısını yapıyor; yeni
sayfayı tek başına tam çeviriye taşımak tutarsızlık yaratır. Tam i18n geçişi
ayrı bir iştir ve bu planın kapsamı dışındadır.

- [ ] **Adım 6: Sözdizimi denetimi**

```bash
node -e "['docs/panel-kurye.html','docs/basvurular.html'].forEach(function(f){var fs=require('fs'),vm=require('vm');var h=fs.readFileSync(f,'utf8');var re=/<script(?![^>]*src=)[^>]*>([\s\S]*?)<\/script>/g,m,i=0;while((m=re.exec(h))){if(!m[1].trim())continue;new vm.Script(m[1],{filename:f+'#'+(++i)});}console.log('OK '+f);})"
node --check docs/assets/js/i18n.js
node --check docs/assets/js/cv.js
```

Beklenen: her iki HTML için `OK`, iki `node --check` sessiz.

- [ ] **Adım 7: Commit**

```bash
git add docs/panel-kurye.html docs/basvurular.html docs/sitemap.xml \
        docs/assets/js/i18n.js docs/assets/js/cv.js docs/assets/js/app.js \
        docs/cv-olustur.html CLAUDE.md
git commit -m "feat(cv): panel giris karti, aday cipleri, i18n ve CLAUDE.md"
```

---

### Görev 9: Uçtan uca doğrulama

Bu görev kod yazmaz; kanıt üretir. Hiçbiri "muhtemelen çalışıyor" ile geçilmez.

**Dosyalar:** yok (yalnız doğrulama)

- [ ] **Adım 1: XSS — CV alanları düz metin basıyor mu**

Sihirbazda özet alanına şunu yaz ve yayınla:

```
<img src=x onerror="window.__pwned=true">
```

Sonra `profil-kurye.html?id=<kendi-id>` sayfasında konsolda:

```js
window.__pwned                                   // undefined olmalı
document.querySelectorAll('.prf-cv-ozet img').length   // 0 olmalı
document.querySelector('.prf-cv-ozet').textContent     // yükü DÜZ METİN göstermeli
```

- [ ] **Adım 2: Boş durum — CV yokken bölüm yok**

CV'si olmayan bir kurye profilinde:

```js
[].slice.call(document.querySelectorAll('.prf-section__h'))
  .map(function(e){ return e.textContent; })
  .indexOf('Özgeçmiş')                          // -1 olmalı
```

- [ ] **Adım 3: Yayınlanmamış CV başkasına görünmüyor**

Kurye CV'yi kaydeder ama **yayınlamaz** (`yayinlandi = false`). Başka bir
hesapla o profili aç. Beklenen: "Özgeçmiş" bölümü yok.

- [ ] **Adım 4: Tek kaynak — iş deneyimi ayrışmıyor**

Sihirbazın 4. adımından bir deneyim ekle. Sonra `profil-duzenle.html` → İş
Deneyimi sekmesini aç. Beklenen: **aynı kayıt orada görünüyor**. Tersini de
dene: `profil-duzenle.html`'den ekle, sihirbazda görün.

- [ ] **Adım 5: Rol kapısı**

Firma ve esnaf hesaplarıyla `cv-olustur.html`'i aç. Beklenen: ikisi de kendi
paneline yönleniyor, sihirbaz hiç görünmüyor.

- [ ] **Adım 6: Bağlantı yokken sahte başarı yok**

Konsolda `SB.isOn = function(){ return false; }` yap, sihirbazda "Yayınla"ya
bas. Beklenen: hata toast'ı çıkıyor, "yayınlandı" mesajı **çıkmıyor**,
yönlendirme olmuyor.

- [ ] **Adım 7: Konsol temiz**

Sihirbazın altı adımını da gez, sonra profili aç. Beklenen: konsolda **0
hata**, 0 CSP ihlali.

- [ ] **Adım 8: Sonucu commit'e yaz**

```bash
git commit --allow-empty -m "test(cv): uctan uca dogrulama - XSS, bos durum, tek kaynak, rol kapisi"
```

---

## Kapsam Dışı (spec'ten devralındı)

- PDF / yazdırma çıktısı
- Paylaşılabilir genel bağlantı (`/cv/<id>`)
- Mobil uygulama (`www/`) — APK derlemesi yok
- Belge yükleme + yönetici onayı
- Adli sicil / sağlık raporu / psikoteknik alanları
- `work_experience` formunun ortak modüle çıkarılması
