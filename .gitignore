# Claude Code yerel ayarları
.claude/settings.local.json

# Claude Code geçici worktree'leri — sitenin komple ikinci bir kopyası.
# Git kayıtlı worktree'leri kendiliğinden dışlar, ama kayıt düşer de
# dosyalar kalırsa "git add -A" hepsini alır. Bu repoda .nojekyll olduğu
# için dotfolder'lar da GitHub Pages'te yayınlanır: o durumda sitenin
# eski bir kopyası kuryemibul.com/.claude/... altında canlıya çıkardı.
.claude/worktrees/

# Bağımlılıklar / derleme çıktıları (statik site için gerekmez ama güvenli)
node_modules/
dist/

# Capacitor — android/ kaynağı repoda tutulur (deep-link manifest özelleştirmesi için);
# build çıktıları android/.gitignore ile elenir. iOS henüz yok.
ios/
.gradle/

# İMZALAMA ANAHTARLARI — asla commit'lenmez.
# Kaybedilirse Play Store'da uygulama bir daha GÜNCELLENEMEZ; ayrı
# ve yedekli bir yerde saklayın (bkz. store/keystore-setup-guide.md).
*.keystore
*.jks
keystore.properties
android/app/release.keystore

# Firebase yapılandırması (proje kimlikleri + API anahtarı içerir)
android/app/google-services.json

# Ortam değişkenleri / sırlar
.env
.env.*
!.env.example

# OS / editör
Thumbs.db
.DS_Store
desktop.ini
.vscode/
.idea/
android/.idea/

# Playwright MCP geçici çıktıları
.playwright-mcp/
