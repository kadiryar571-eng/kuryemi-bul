{
  "name": "kuryemibul-app",
  "version": "1.0.0",
  "private": true,
  "description": "Kuryemi Bul — Capacitor native kabuk (canlı siteyi yükler)",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
